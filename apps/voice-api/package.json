{
  "name": "@kural-sevi/voice-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "PYTHONPATH=. .venv/bin/uvicorn main:app --reload --port 8000",
    "test": "PYTHONPATH=. .venv/bin/python -m unittest discover tests"
  }
}
